# HeroesAPI_golang_gorm
REST Api built in golang with use of gorm and gin.
Simple project built to test golang and web applications skill. 
To run project: 
docker compose up 
From main directory 
To run tests:
cd tests && go test
From main directory.
